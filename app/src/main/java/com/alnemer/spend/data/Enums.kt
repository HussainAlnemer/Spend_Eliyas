package com.alnemer.spend.data

// Classification provenance — precedence order is the ordinal order (lower wins).
enum class ClassifiedBy { MANUAL, RECONCILED, MERCHANT_RULE, PATTERN_RULE, SEED, MODEL, NONE }

enum class AccountType { CURRENT, CREDIT_CARD, WALLET, FX_WALLET, GOLD, INVESTMENT, CASH }
enum class BalanceSemantics { RUNNING_BALANCE, AVAILABLE_CREDIT, WALLET_BALANCE }
enum class Direction { DEBIT, CREDIT }
enum class TxnStatus { PROVISIONAL, CONFIRMED }
enum class SourceKind { SMS, PUSH, STATEMENT_ROW, EXPORT_ROW, MANUAL, PASTE }
enum class MsgClass { TRANSACTION, OTP, PROMO, INFO, UNPARSED }

enum class TxnType {
    PURCHASE, REFUND, TRANSFER_OUT, TRANSFER_IN, TRANSFER_TO_WALLET,
    WALLET_TOPUP_RECEIVED, CREDIT_CARD_PAYMENT, CARD_PAYMENT_RECEIVED,
    CASH_WITHDRAWAL, BILL_PAYMENT_SADAD, FEE, REBATE, REVERSAL,
    INSTALLMENT_BILLING, INCOME_SALARY, INCOME_INVESTMENT, TAWARRUQ, UNKNOWN
}

enum class LinkType {
    TRANSFER_PAIR, CARD_PAYMENT, WALLET_TOPUP, REFUND_OF, REBATE_OF,
    REVERSAL_OF, INSTALLMENT_ORIGIN
}
enum class LinkMethod { DETERMINISTIC, FUZZY_AUTO, FUZZY_CONFIRMED, MANUAL }
enum class ReconStatus { PASSED, FAILED, PARTIAL, PENDING }
